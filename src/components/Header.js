import React from 'react';

function Header() {
    return (
        <nav className="App-header">
            <div className="nav-wrapper">
                <h2 className="App-title">React Salário</h2>
            </div>
        </nav>
    );
}

export default Header;
